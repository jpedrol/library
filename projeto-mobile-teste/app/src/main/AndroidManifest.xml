<?xml version="1.0" encoding="utf-8"?>
    <manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

        <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Library">

            <!-- Tela de LOGIN (inicial) -->
            <activity
                android:name=".LoguinActivity"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

            <!-- Tela de REGISTRO -->
            <activity
                android:name=".RegisterActivity"
                android:exported="false"
                android:parentActivityName=".LoguinActivity">
                <!-- Compatibilidade com versões antigas -->
                <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".LoguinActivity" />
            </activity>

            <activity android:name=".MenuInicialActivity" />

            <activity android:name=".TelaLivroDetalheActivity" />


        </application>
    </manifest>
