<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- tamanho do ponto -->
    <size android:width="12dp" android:height="12dp"/>

    <!-- cor do ponto ativo (branco) -->
    <solid android:color="@android:color/white"/>
</shape>
